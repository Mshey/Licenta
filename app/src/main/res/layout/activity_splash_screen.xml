<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/splash_screen_imageView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center"
        android:scaleType="fitXY"
        app:srcCompat="@drawable/background_airplane" />

    <!--    <ProgressBar-->
    <!--        android:id="@+id/progressBar"-->
    <!--        style="?android:attr/progressBarStyleHorizontal"-->
    <!--        android:layout_width="@dimen/progressBarWidth"-->
    <!--        android:layout_height="@dimen/progressBarHeight"-->
    <!--        android:layout_marginBottom="@dimen/progressBarMarginBottom"-->
    <!--        android:progressDrawable="@drawable/progress_bar_drawable"-->
    <!--        app:layout_constraintBottom_toBottomOf="@id/splash_screen_imageView"-->
    <!--        app:layout_constraintEnd_toEndOf="parent"-->
    <!--        app:layout_constraintHorizontal_bias="@dimen/progressBarConstraintHorizontalBias"-->
    <!--        app:layout_constraintStart_toStartOf="parent" />-->

    <ProgressBar
        android:id="@+id/progressBar"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="@dimen/progressBarWidth"
        android:layout_height="@dimen/progressBarHeight"
        android:layout_marginBottom="@dimen/progressBarMarginBottom"
        android:max="100"
        android:progress="1"
        android:progressDrawable="@drawable/progress_bar_drawable"
        app:layout_constraintBottom_toBottomOf="@id/splash_screen_imageView"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="@dimen/progressBarConstraintHorizontalBias"
        app:layout_constraintStart_toStartOf="parent" />
</androidx.constraintlayout.widget.ConstraintLayout>